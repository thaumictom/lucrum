services:
  lucrum:
    build: .
    env_file: .env
    environment:
      DATA_DIR: /data
      HTTP_ADDR: ':3003'
    ports:
      - '3003:3003'
    volumes:
      - lucrum-data:/data
    restart: unless-stopped
    stop_grace_period: 30s

volumes:
  lucrum-data:
