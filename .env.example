# Fetch immediately on startup, then every 180 minutes.
FETCH_INTERVAL_MINUTES=180
DATA_DIR=./data

# Shared across catalogue and statistics requests; never greater than 3.
WFM_REQUESTS_PER_SECOND=2.5
# Log every fetch and scheduling skip.
DEBUG=false
